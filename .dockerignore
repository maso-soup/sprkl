.venv/
instance/
__pycache__/
*.pyc
.pytest_cache/
.git/
/tmp/
# The app image builds from an explicit COPY allowlist (app/, shared/, serve.py),
# so the answer key never reaches it even by accident. Listed here too as defence
# in depth for anyone who changes the Dockerfile to `COPY . .`:
findings.yaml
scorer/
tests/
tools/
CHEATSHEET.md
cheatsheet.html
README.md
